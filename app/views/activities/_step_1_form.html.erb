<%= form_with(model: activity) do |form| %>

  <%= render "error_display", activity: activity %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "form-input" %>
  </div>

  <div>
    <%= form.submit "Next", class: "btn"  %>
  </div>

<% end %>
